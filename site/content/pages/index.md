# CAP

CAP is an open protocol for capability disclosure, invocation, and provenance in causal reasoning systems.

CAP standardizes:

- capability discovery
- causal verb semantics
- conformance levels
- assumptions disclosure
- provenance and transport bindings

CAP does not standardize:

- how a causal engine discovers graphs
- how data is ingested, stored, or refreshed
- agent orchestration
- implementation-specific discovery surfaces
- which causal engine is scientifically correct

Primary destinations:

- `/docs`
- `/docs/overview`
- `/docs/getting-started`
- `/docs/quickstart-client`
- `/docs/quickstart-server`
- `/spec`
- `/contributing`
- `/governance`

The route-to-source mapping for the site lives in `site/content/site-map.json`.

This page should stay aligned with the repository markdown sources in `documentation/`, `specification/`, and the root `README.md`.
